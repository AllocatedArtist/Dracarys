#version 300 es
in vec3 aPos;
in vec3 aColor;

out vec3 color;

uniform vec2 offset_pos;

void main() {
    gl_Position = vec4((aPos * 0.1) + vec3(offset_pos, 0.0), 1.0);

    color = aColor;
}

$#version 300 es

precision mediump float;
out vec4 fragColor;

in vec3 color;

void main() {
    fragColor = vec4(color, 1.0);
}